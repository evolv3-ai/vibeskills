# Skills Catalog

**60 production-ready skills** organized by category.

Each skill includes auto-trigger keywords, error prevention counts, and links to full documentation.

---

## Cloudflare Platform (16 skills)

### cloudflare-worker-base
Foundation for Cloudflare Workers with Hono routing, Static Assets, and Vite plugin. **Prevents 6 errors.**

**Triggers**: `cloudflare worker`, `hono`, `workers static assets`, `vite plugin`

---

### cloudflare-d1
D1 serverless SQL database with migrations, prepared statements, and batch queries. **Prevents 6 errors.**

**Triggers**: `d1 database`, `cloudflare d1`, `sql workers`, `d1 migrations`

---

### cloudflare-r2
R2 object storage (S3-compatible) with multipart uploads, presigned URLs, and CORS. **Prevents 6 errors.**

**Triggers**: `r2 storage`, `cloudflare r2`, `object storage`, `r2 upload`

---

### cloudflare-kv
KV key-value storage with TTL, metadata, bulk operations, and caching patterns. **Prevents 6 errors.**

**Triggers**: `kv storage`, `cloudflare kv`, `kv cache`, `workers kv`

---

### cloudflare-workers-ai
Workers AI integration with 50+ models: LLMs, embeddings, image generation, vision. **Prevents 6 errors.**

**Triggers**: `workers ai`, `cloudflare ai`, `llm workers`, `embeddings`

---

### cloudflare-vectorize
Vector database for RAG and semantic search with Workers AI and OpenAI embeddings. **Prevents 8 errors.**

**Triggers**: `vectorize`, `vector search`, `rag cloudflare`, `semantic search`

---

### cloudflare-queues
Message queues for async processing with batching, retries, and dead letter queues. **Prevents 8 errors.**

**Triggers**: `cloudflare queues`, `message queue`, `async processing`, `background jobs`

---

### cloudflare-workflows
Durable execution for multi-step applications with automatic retries and state persistence. **Prevents 5 errors.**

**Triggers**: `cloudflare workflows`, `durable execution`, `workflow step`, `long-running tasks`

---

### cloudflare-durable-objects
Stateful coordination with WebSocket Hibernation, SQL storage, alarms, and real-time patterns. **Prevents 18 errors.**

**Triggers**: `durable objects`, `websocket hibernation`, `do state api`, `real-time cloudflare`

---

### cloudflare-agents
Complete Agents SDK for building stateful AI agents with WebSocket, scheduling, RAG, and MCP servers. **Prevents 15 errors.**

**Triggers**: `cloudflare agents`, `agents sdk`, `stateful agents`, `mcp server`

---

### cloudflare-mcp-server
Build remote Model Context Protocol servers on Cloudflare Workers. Includes OAuth, Durable Objects, WebSocket hibernation. **Prevents 15 errors. 87% token savings.**

**Triggers**: `mcp server cloudflare`, `model context protocol`, `remote mcp`, `mcp oauth`

---

### cloudflare-turnstile
CAPTCHA-alternative bot protection with client-side widgets and server-side validation. **Prevents 12 errors.**

**Triggers**: `turnstile`, `captcha`, `bot protection`, `recaptcha alternative`

---

### cloudflare-hyperdrive
Connection pooling for Postgres and MySQL databases. **Prevents 6 errors.**

**Triggers**: `hyperdrive`, `database pooling`, `postgres cloudflare`

---

### cloudflare-images
Image optimization and delivery. **Prevents 4 errors.**

**Triggers**: `cloudflare images`, `image optimization`, `image delivery`

---

### cloudflare-browser-rendering
Headless browser automation with Puppeteer for Workers. **Prevents 8 errors.**

**Triggers**: `browser rendering`, `puppeteer workers`, `headless browser`

---

### drizzle-orm-d1
Drizzle ORM integration with Cloudflare D1 for type-safe database queries. **Prevents 8 errors.**

**Triggers**: `drizzle orm`, `drizzle d1`, `type-safe queries`, `orm cloudflare`

---

## AI & Machine Learning (12 skills)

### ai-sdk-core
Backend AI with Vercel AI SDK v5: text generation, structured output, tool calling, agents. **Prevents 12 errors.**

**Triggers**: `ai sdk core`, `vercel ai sdk`, `generateText`, `streamText`

---

### ai-sdk-ui
Frontend React hooks (useChat, useCompletion, useObject) for AI-powered UIs. **Prevents 12 errors.**

**Triggers**: `ai sdk ui`, `useChat`, `react ai chat`, `streaming ai ui`

---

### openai-api
OpenAI API integration: chat completions, embeddings, vision, audio. **Prevents 8 errors.**

**Triggers**: `openai api`, `chatgpt api`, `gpt-4`, `openai sdk`

---

### openai-agents
OpenAI Agents SDK for building stateful agents with tools and handoffs. **Prevents 12 errors.**

**Triggers**: `openai agents`, `agent sdk`, `openai tools`, `agent handoffs`

---

### openai-assistants
OpenAI Assistants API for long-running conversations with memory. **Prevents 10 errors.**

**Triggers**: `openai assistants`, `assistants api`, `openai threads`

---

### openai-responses
OpenAI Responses API for structured outputs. **Prevents 6 errors.**

**Triggers**: `openai responses`, `structured output`, `response format`

---

### google-gemini-api
Google Gemini API integration: multimodal AI, vision, reasoning. **Prevents 8 errors.**

**Triggers**: `gemini api`, `google ai`, `gemini pro`, `multimodal ai`

---

### google-gemini-embeddings
Google Gemini embeddings for RAG and semantic search. **Prevents 6 errors.**

**Triggers**: `gemini embeddings`, `text-embedding`, `google embeddings`

---

### claude-api
Anthropic Claude API integration for advanced reasoning. **Prevents 8 errors.**

**Triggers**: `claude api`, `anthropic api`, `claude sonnet`

---

### claude-agent-sdk
Claude Agent SDK for building agentic applications. **Prevents 10 errors.**

**Triggers**: `claude agent sdk`, `anthropic agents`, `claude tools`

---

### elevenlabs-agents
ElevenLabs conversational AI agents with voice synthesis. **Prevents 6 errors.**

**Triggers**: `elevenlabs`, `voice ai`, `conversational agents`, `text to speech`

---

### thesys-generative-ui
Thesys generative UI for dynamic, AI-powered interfaces. **Prevents 8 errors.**

**Triggers**: `thesys`, `generative ui`, `dynamic ui`, `ai components`

---

## Frontend & UI (12 skills)

### tailwind-v4-shadcn
Tailwind CSS v4 + shadcn/ui + Vite + React with dark mode and theme provider. **Prevents 9 errors.**

**Triggers**: `tailwind v4`, `shadcn/ui`, `dark mode`, `@theme inline`

---

### react-hook-form-zod
Forms with React Hook Form and Zod validation for type-safe form handling. **Prevents 8 errors.**

**Triggers**: `react hook form`, `zod validation`, `form handling`, `useForm`

---

### tanstack-query
Server state management with TanStack Query for caching and data fetching. **Prevents 10 errors.**

**Triggers**: `tanstack query`, `react query`, `useQuery`, `data fetching`

---

### tanstack-router
Type-safe routing with TanStack Router. **Prevents 8 errors.**

**Triggers**: `tanstack router`, `type-safe routing`, `file-based routing`

---

### tanstack-start
Full-stack React framework with TanStack Start. **Prevents 8 errors.**

**Triggers**: `tanstack start`, `full-stack react`, `server functions`

---

### tanstack-table
Headless table components with TanStack Table. **Prevents 6 errors.**

**Triggers**: `tanstack table`, `react table`, `data grid`, `sorting filtering`

---

### zustand-state-management
Client state management with Zustand: simple, performant, TypeScript-first. **Prevents 6 errors.**

**Triggers**: `zustand`, `state management`, `global state`, `react state`

---

### hono-routing
Complete Hono routing and middleware patterns: validation, RPC, error handling. **Prevents 8 errors.**

**Triggers**: `hono routing`, `hono middleware`, `hono validator`, `hono rpc`

---

### nextjs
Next.js App Router patterns and best practices. **Prevents 12 errors.**

**Triggers**: `next.js`, `app router`, `server components`, `nextjs patterns`

---

### motion
Animation library for React (formerly Framer Motion). **Prevents 6 errors.**

**Triggers**: `motion`, `framer motion`, `react animation`, `gesture`

---

### auto-animate
Automatic animations for web apps with zero config. **Prevents 4 errors.**

**Triggers**: `auto-animate`, `automatic animations`, `list animations`

---

### tiptap
Rich text editor framework based on ProseMirror. **Prevents 8 errors.**

**Triggers**: `tiptap`, `rich text editor`, `prosemirror`, `wysiwyg`

---

## Authentication (2 skills)

### clerk-auth
Complete Clerk authentication for React, Next.js, and Cloudflare Workers. **Prevents 10 errors.**

**Triggers**: `clerk auth`, `clerk`, `@clerk/nextjs`, `@clerk/backend`

---

### better-auth
Framework-agnostic authentication with Better Auth. **Prevents 8 errors.**

**Triggers**: `better-auth`, `authentication`, `oauth`, `session management`

---

## Database & Storage (4 skills)

### neon-vercel-postgres
Serverless Postgres for edge/serverless environments with Neon. **Prevents 15 errors.**

**Triggers**: `neon postgres`, `vercel postgres`, `serverless postgres`, `postgres edge`

---

### vercel-kv
Redis-compatible key-value storage for caching and sessions. **Prevents 10 errors.**

**Triggers**: `vercel kv`, `@vercel/kv`, `vercel redis`, `rate limiting`

---

### vercel-blob
Object storage with automatic CDN for file uploads. **Prevents 10 errors.**

**Triggers**: `vercel blob`, `@vercel/blob`, `file upload`, `image upload`

---

## Content Management (3 skills)

### tinacms
TinaCMS for Git-backed content management. **Prevents 9 errors.**

**Triggers**: `tinacms`, `tina cms`, `git cms`, `content management`

---

### sveltia-cms
Sveltia CMS for lightweight, Git-based content editing. **Prevents 6 errors.**

**Triggers**: `sveltia cms`, `netlify cms alternative`, `git based cms`

---

### wordpress-plugin-core
WordPress plugin development with security foundation and 3 architecture patterns. **Prevents 20+ security vulnerabilities.**

**Triggers**: `wordpress plugin`, `wordpress development`, `plugin security`, `wordpress rest api`

---

## Python (2 skills)

### fastapi
Python APIs with FastAPI, Pydantic v2, SQLAlchemy async, JWT auth. **Prevents 10 errors.**

**Triggers**: `fastapi`, `python api`, `pydantic`, `uvicorn`, `422 error`

---

### flask
Python web apps with Flask, Blueprints, Flask-Login, application factory. **Prevents 8 errors.**

**Triggers**: `flask`, `flask blueprints`, `flask-login`, `circular import`

---

## MCP & Tooling (4 skills)

### typescript-mcp
TypeScript MCP server development for Cloudflare Workers. **Prevents 8 errors.**

**Triggers**: `typescript mcp`, `mcp server`, `model context protocol`

---

### fastmcp
FastMCP Python framework for MCP servers with storage backends and OAuth. **Prevents 25 errors. 90-95% token savings.**

**Triggers**: `fastmcp`, `python mcp`, `mcp server`, `storage backends`

---

### ts-agent-sdk
TypeScript Agent SDK patterns. **Prevents 6 errors.**

**Triggers**: `ts agent sdk`, `typescript agents`, `agent patterns`

---

### openai-apps-mcp
OpenAI apps with MCP integration. **Prevents 6 errors.**

**Triggers**: `openai mcp`, `openai apps`, `mcp integration`

---

## Project Planning & Workflow (5 skills)

### project-workflow
7 integrated slash commands for complete project lifecycle. **Saves 35-55 minutes per project.**

**Commands**: `/explore-idea`, `/plan-project`, `/plan-feature`, `/wrap-session`, `/continue-session`, `/workflow`, `/release`

**Triggers**: `project workflow`, `workflow commands`, `project lifecycle`

---

### project-planning
Structured planning with IMPLEMENTATION_PHASES.md generation. **Prevents 4 errors.**

**Triggers**: `project planning`, `implementation phases`, `planning docs`

---

### project-session-management
Session handoff protocol for managing context across sessions. **Prevents 3 errors.**

**Triggers**: `session management`, `session handoff`, `context management`

---

### skill-creator
Design effective Claude Code skills with optimal descriptions and patterns. **Meta-skill.**

**Triggers**: `create skill`, `skill design`, `skill description`

---

### open-source-contributions
Guidelines for contributing to open source projects. **Best practices.**

**Triggers**: `open source`, `contributing`, `pull request`, `github workflow`

---

## Quick Reference

| Category | Count | Key Skills |
|----------|-------|------------|
| Cloudflare | 16 | worker-base, d1, r2, kv, agents, mcp-server |
| AI/ML | 12 | ai-sdk-core/ui, openai-agents, claude-api |
| Frontend | 12 | tailwind-v4-shadcn, tanstack-*, zustand |
| Auth | 2 | clerk-auth, better-auth |
| Database | 4 | neon-postgres, vercel-kv/blob, drizzle |
| CMS | 3 | tinacms, sveltia-cms, wordpress |
| Python | 2 | fastapi, flask |
| MCP/Tools | 4 | fastmcp, typescript-mcp |
| Planning | 5 | project-workflow, project-planning |

**Total: 60 skills | 400+ errors prevented | ~50% average token savings**

---

## Installation

```bash
# Marketplace (recommended)
/plugin install cloudflare-worker-base@claude-skills

# Manual
./scripts/install-skill.sh cloudflare-worker-base
```

See [README.md](README.md) for full installation instructions.
